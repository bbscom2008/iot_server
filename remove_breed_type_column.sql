-- 删除 devices 表中的 breed_type 列的迁移脚本
-- 执行时间：迁移前请先备份数据库

-- ============================================
-- 第一步：删除 breed_type 列
-- ============================================

ALTER TABLE devices DROP COLUMN IF EXISTS breed_type;

-- ============================================
-- 第二步：验证删除结果
-- ============================================

DESC devices;

SELECT 'breed_type 列已成功删除' as '';

