# 字典字段修改完成 ✅

## 🔄 数据库变化

根据你修改的 `iot_database_init.sql`，字段类型已从字符串改为整数（关联字典表）：

### 修改对照表

| 字段 | 之前 | 现在 | 说明 |
|------|------|------|------|
| `breeding_type` | VARCHAR(20) | **INT** | 养殖类型改为数字（关联字典） |
| `position` | VARCHAR(20) | **INT（改名为 role）** | 岗位改为角色，使用数字 |

---

## 📊 字典映射关系

### 养殖类型 (breed_type)

| 显示名称 | 字典值 | 存储值 |
|---------|--------|--------|
| 猪 | "0" | 0 |
| 羊 | "1" | 1 |
| 牛 | "2" | 2 |
| 鸡 | "3" | 3 |
| 鸭 | "4" | 4 |
| 其他 | "5" | 5 |

### 角色类型 (role_type)

| 显示名称 | 字典值 | 存储值 |
|---------|--------|--------|
| 老板 | "0" | 0 |
| 饲养员 | "1" | 1 |
| 其他 | "2" | 2 |

---

## ✅ 已同步修改的代码

### 1. User 实体类

**文件：** `src/main/java/com/example/demo/entity/User.java`

```java
private Integer breedingType;   // 0-猪 1-羊 2-牛 3-鸡 4-鸭 5-其他
private Integer role;           // 0-老板 1-饲养员 2-其他
```

### 2. UserMapper.xml

**字段映射：**
```xml
<result column="breeding_type" property="breedingType" jdbcType="INTEGER"/>
<result column="role" property="role" jdbcType="INTEGER"/>
```

**INSERT 语句：**
```xml
INSERT INTO users (phone, password, nike_name, breeding_type, role, created_at)
VALUES (#{phone}, #{password}, #{nikeName}, #{breedingType}, #{role}, NOW())
```

### 3. RegisterRequest DTO

```java
@NotNull(message = "养殖类型不能为空")
private Integer breedingType;  // 0-猪 1-羊 2-牛 3-鸡 4-鸭 5-其他

@NotNull(message = "角色不能为空")
private Integer role;  // 0-老板 1-饲养员 2-其他
```

### 4. UserService

```java
user.setBreedingType(request.getBreedingType()); // Integer 类型
user.setRole(request.getRole()); // Integer 类型
```

### 5. 数据库更新脚本

`update_users_table.sql` - 改为 INT 类型

---

## 🔧 前端需要修改

### ⚠️ 重要：前端需要传递整数而不是字符串

**前端注册页面需要修改：**

**文件：** `C:\Users\Administrator\Desktop\server\dspace\src\pages\register\register.vue`

**之前的代码（第 147-152 行）：**
```javascript
const res = await request.post('user/register', {
    phone: this.phone,
    password: this.password,
    verifyCode: this.verifyCode,
    breedingType: this.breedingTypes[this.breedingTypeIndex],  // ❌ 字符串 "养猪"
    position: this.positions[this.positionIndex]  // ❌ 字符串 "老板"
})
```

**应该改为：**
```javascript
const res = await request.post('user/register', {
    phone: this.phone,
    password: this.password,
    verifyCode: this.verifyCode,
    breedingType: this.breedingTypeIndex,  // ✅ 整数 0/1/2/3/4/5
    role: this.positionIndex  // ✅ 整数 0/1/2（注意改为 role）
})
```

---

## 📝 前端修改详细说明

### 修改 register.vue

**找到注册方法（约第 100 行）：**

```javascript
// 注册
async handleRegister() {
    // ... 验证代码 ...

    try {
        const res = await request.post('user/register', {
            phone: this.phone,
            password: this.password,
            verifyCode: this.verifyCode,
            breedingType: this.breedingTypeIndex,  // 改：使用索引（整数）
            role: this.positionIndex  // 改：字段名改为 role，使用索引
        })
        console.log("====== register ====", res);
        
        uni.showToast({
            title: '注册成功',
            icon: 'success'
        })
        setTimeout(() => {
            uni.navigateBack()
        }, 1500)
    } catch (err) {
        uni.showToast({
            title: err.msg || '注册失败',
            icon: 'none'
        })
    }
},
```

### 数据对应关系

**养殖类型：**
```javascript
breedingTypes: ['养猪', '养鸭', '养鸡', '养兔', '其他']
//               索引 0     1      2      3      4

// 用户选择"养猪" → breedingTypeIndex = 0 → 传递 0 给后端
// 用户选择"养鸭" → breedingTypeIndex = 1 → 传递 1 给后端
```

**角色：**
```javascript
positions: ['老板', '饲养员', '其他']
//          索引 0     1        2

// 用户选择"老板" → positionIndex = 0 → 传递 0 给后端（字段名改为 role）
// 用户选择"饲养员" → positionIndex = 1 → 传递 1 给后端
```

---

## 🧪 测试验证

### 后端测试（使用整数）

```bash
# 注册测试（使用整数）
curl -X POST http://localhost:8080/user/register \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "13711111111",
    "password": "123456",
    "verifyCode": "123456",
    "breedingType": 0,
    "role": 0
  }'
```

**预期响应：**
```json
{
  "code": 200,
  "msg": "注册成功",
  "data": null
}
```

---

## 📋 修改清单

### 后端代码（已完成）✅

- [x] `User.java` - breedingType 和 role 改为 Integer
- [x] `UserMapper.xml` - 字段类型改为 INTEGER
- [x] `RegisterRequest.java` - 字段类型改为 Integer，position 改为 role
- [x] `UserService.java` - 使用 role 字段
- [x] `update_users_table.sql` - INT 类型

### 前端代码（需要修改）⚠️

- [ ] `register.vue` - 传递索引（整数）而不是字符串
- [ ] `register.vue` - 字段名 position 改为 role

---

## 🔧 前端修改指南

### 修改位置

**文件：** `C:\Users\Administrator\Desktop\server\dspace\src\pages\register\register.vue`

**第 147-152 行，修改为：**

```javascript
const res = await request.post('user/register', {
    phone: this.phone,
    password: this.password,
    verifyCode: this.verifyCode,
    breedingType: this.breedingTypeIndex,  // 传递整数索引
    role: this.positionIndex  // 字段名改为 role，传递整数索引
})
```

**完整的修改：**
```vue
// 注册
async handleRegister() {
    // 验证养殖类型
    if (this.breedingTypeIndex < 0) {
        uni.showToast({
            title: '请选择养殖类型',
            icon: 'none'
        })
        return
    }
    
    // 验证岗位
    if (this.positionIndex < 0) {
        uni.showToast({
            title: '请选择岗位',
            icon: 'none'
        })
        return
    }
    
    // 验证手机号
    if (!this.phone) {
        uni.showToast({
            title: '请输入手机号',
            icon: 'none'
        })
        return
    }
    
    // 验证验证码
    if (!this.verifyCode) {
        uni.showToast({
            title: '请输入验证码',
            icon: 'none'
        })
        return
    }
    
    // 验证密码
    if (!this.password) {
        uni.showToast({
            title: '请输入密码',
            icon: 'none'
        })
        return
    }

    try {
        const res = await request.post('user/register', {
            phone: this.phone,
            password: this.password,
            verifyCode: this.verifyCode,
            breedingType: this.breedingTypeIndex,  // ← 改：传递整数
            role: this.positionIndex  // ← 改：字段名和值
        })
        console.log("====== register ====", res);
        
        uni.showToast({
            title: '注册成功',
            icon: 'success'
        })
        setTimeout(() => {
            uni.navigateBack()
        }, 1500)
    } catch (err) {
        uni.showToast({
            title: err.msg || '注册失败',
            icon: 'none'
        })
    }
},
```

---

## 🚀 更新步骤

### 第一步：更新数据库

```bash
# 如果数据库已存在，需要修改字段类型
mysql -u root -p

USE iot_system;

# 删除旧字段（如果存在）
ALTER TABLE users DROP COLUMN IF EXISTS breeding_type;
ALTER TABLE users DROP COLUMN IF EXISTS position;

# 重新添加正确类型的字段
ALTER TABLE users ADD COLUMN breeding_type INT COMMENT '养殖类型：字典中的 breed_type' AFTER icon;
ALTER TABLE users ADD COLUMN role INT COMMENT '角色：字典中的 role_type' AFTER breeding_type;

# 为现有用户设置默认值
UPDATE users SET breeding_type = 0, role = 0 WHERE breeding_type IS NULL;
```

或者直接重新创建数据库：
```bash
mysql -u root -p

DROP DATABASE IF EXISTS iot_system;
source iot_database_init.sql;
```

### 第二步：重启后端

```bash
mvn spring-boot:run
```

### 第三步：修改前端

修改 `register.vue` 文件（见上面的修改指南）

### 第四步：重启前端

```bash
cd C:\Users\Administrator\Desktop\server\dspace
npm run dev:h5
```

---

## 🧪 测试

### 后端测试（使用整数）

```http
POST http://localhost:8080/user/register
Content-Type: application/json

{
  "phone": "13711111111",
  "password": "123456",
  "verifyCode": "123456",
  "breedingType": 0,
  "role": 0
}
```

**breedingType: 0** 表示"猪"  
**role: 0** 表示"老板"

---

## 📋 前后端字段对应

### 前端选择 → 后端存储

| 用户选择 | 前端索引 | 后端字段 | 后端值 | 字典表 |
|---------|---------|---------|--------|--------|
| 养猪 | 0 | breedingType | 0 | breed_type |
| 养鸭 | 1 | breedingType | 1 | breed_type |
| 养鸡 | 2 | breedingType | 2 | breed_type |
| 养兔 | 3 | breedingType | 3 | breed_type |
| 其他 | 4 | breedingType | 5 | breed_type |
| 老板 | 0 | role | 0 | role_type |
| 饲养员 | 1 | role | 1 | role_type |
| 其他 | 2 | role | 2 | role_type |

---

## ⚠️ 重要提示

### 前端数组索引与后端值的映射

**注意：前端的"其他"是索引 4，但后端的"其他"是值 5！**

**前端需要做映射：**

```javascript
// 养殖类型映射
const breedingTypeMap = {
    0: 0,  // 养猪 → 0
    1: 1,  // 养鸭 → 1（注意：数据库中鸭是1，不是4）
    2: 2,  // 养鸡 → 2
    3: 3,  // 养兔 → 3
    4: 5   // 其他 → 5
}

// 角色映射（一致，无需映射）
// 0 → 0, 1 → 1, 2 → 2
```

**但是！** 从字典数据看，前端的顺序和字典不一致！

### 前端数组和字典的差异

**前端：**
```javascript
breedingTypes: ['养猪', '养鸭', '养鸡', '养兔', '其他']
//              索引 0     1      2      3      4
```

**字典：**
```sql
('breed_type', '猪', '0', 1)   -- 0
('breed_type', '羊', '1', 2)   -- 1
('breed_type', '牛', '2', 3)   -- 2
('breed_type', '鸡', '3', 4)   -- 3
('breed_type', '鸭', '4', 5)   -- 4
('breed_type', '其他', '5', 6) -- 5
```

**建议：前端改为动态从字典获取！**

---

## 🎯 推荐方案：前端动态获取字典

### 步骤1：前端从后端获取字典数据

**在 register.vue 的 onLoad 中：**

```javascript
data() {
    return {
        phone: '',
        password: '',
        verifyCode: '',
        countdown: 0,
        // 养殖类型和角色从后端获取
        breedingTypes: [],  // 改为空数组
        breedingTypeValues: [],  // 存储对应的值
        breedingTypeIndex: -1,
        positions: [],  // 改为空数组
        positionValues: [],  // 存储对应的值
        positionIndex: -1
    }
},

async onLoad() {
    // 获取养殖类型字典
    try {
        const breedRes = await request.get('user/dict/data/list', { dictType: 'breed_type' })
        this.breedingTypes = breedRes.data.map(item => item.dictLabel)
        this.breedingTypeValues = breedRes.data.map(item => parseInt(item.dictValue))
    } catch (e) {
        console.error('获取养殖类型失败', e)
    }
    
    // 获取角色字典
    try {
        const roleRes = await request.get('user/dict/data/list', { dictType: 'role_type' })
        this.positions = roleRes.data.map(item => item.dictLabel)
        this.positionValues = roleRes.data.map(item => parseInt(item.dictValue))
    } catch (e) {
        console.error('获取角色类型失败', e)
    }
},

// 注册方法
async handleRegister() {
    // ... 验证代码 ...
    
    try {
        const res = await request.post('user/register', {
            phone: this.phone,
            password: this.password,
            verifyCode: this.verifyCode,
            breedingType: this.breedingTypeValues[this.breedingTypeIndex],  // 使用对应的值
            role: this.positionValues[this.positionIndex]  // 使用对应的值
        })
        // ...
    }
}
```

---

## 🎨 简化方案（临时）

如果不想修改前端太多，可以用固定映射：

```javascript
// 在 register.vue 中添加映射方法
methods: {
    // 养殖类型映射
    getBreedingTypeValue() {
        const types = ['养猪', '养鸭', '养鸡', '养兔', '其他']
        const values = [0, 4, 3, -1, 5]  // 对应字典中的值
        // 养猪→0, 养鸭→4, 养鸡→3, 其他→5
        // 注意：养兔在字典中不存在，需要处理
        return values[this.breedingTypeIndex]
    },
    
    // 角色映射（一致）
    getRoleValue() {
        // 老板→0, 饲养员→1, 其他→2
        return this.positionIndex
    },
    
    // 注册
    async handleRegister() {
        // ...
        const res = await request.post('user/register', {
            phone: this.phone,
            password: this.password,
            verifyCode: this.verifyCode,
            breedingType: this.getBreedingTypeValue(),
            role: this.getRoleValue()
        })
        // ...
    }
}
```

---

## 📁 修改的文件

### 后端（已完成）✅

| 文件 | 状态 |
|------|------|
| `User.java` | ✅ |
| `UserMapper.xml` | ✅ |
| `RegisterRequest.java` | ✅ |
| `UserService.java` | ✅ |
| `update_users_table.sql` | ✅ |

### 前端（需要修改）⚠️

| 文件 | 需要修改的内容 |
|------|---------------|
| `register.vue` | breedingType 和 role 传递整数 |

---

## 🚀 立即应用

### 1. 重新初始化数据库

```bash
mysql -u root -p

DROP DATABASE IF EXISTS iot_system;
source iot_database_init.sql;
exit
```

### 2. 重启后端

```bash
mvn spring-boot:run
```

### 3. 测试后端接口

```bash
curl -X POST http://localhost:8080/user/register \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "13711111111",
    "password": "123456",
    "verifyCode": "123456",
    "breedingType": 0,
    "role": 0
  }'
```

### 4. 修改前端 register.vue

按照上面的方案修改前端代码

### 5. 重启前端测试

```bash
cd C:\Users\Administrator\Desktop\server\dspace
npm run dev:h5
```

---

## 🎉 修改完成！

后端代码已完全同步你的数据库修改：

✅ **breedingType** - 从字符串改为整数  
✅ **position** - 改名为 **role** 并改为整数  
✅ **关联字典表** - 使用字典值存储  
✅ **所有相关代码已同步** - 实体类、Mapper、DTO、Service  

**现在需要修改前端，传递整数而不是字符串！** 🔧
